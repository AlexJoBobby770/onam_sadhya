# Database connection (SQLite default for quick local dev, replace with PostgreSQL for production)
DATABASE_URL=sqlite+aiosqlite:///./onam_sadhya.db
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/onam_db

# Secret key for JWT tokens and HMAC QR code signatures
SECRET_KEY=super_secret_onam_sadhya_key_2026_change_in_production

# OTP configuration
OTP_EXPIRY_MINUTES=5
DEV_MODE=true

# Server details
PORT=8000
HOST=0.0.0.0
